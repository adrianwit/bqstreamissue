Streaming control test showing the test data (5 x 1000 rows) can be loaded with stream API,
after recreating table only (no dataset) (CREATE OR REPLACE) inserted data is reported as 0 when queried,
no error is reported


